{
  "name": "vihos-backend",
  "version": "1.0.0",
  "description": "ViHOS Toxic Spans Guard FastAPI Backend",
  "scripts": {
    "dev": "python -m uvicorn main:app --port 8000",
    "start": "python -m uvicorn main:app --port 8000"
  }
}
